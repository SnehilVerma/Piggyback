package com.fivetwenty.piggyback.model;

public class Passenger extends User {

    public Passenger(String userId, String userName, Float userRating, UserType userType) {
        super(userId, userName, userRating, userType);
    }
}
